config mode 'setup'
        option dir '/etc/netmodes'
        option detail 'To change network configuration to preconfigured please select configuration mode from dropdown menu and press button'
        option curmode 'default'

config netmode 'default'
        option desc 'Default'
        option conf 'config_default'
        option exp 'In default configuration WLAN, LAN1 and LAN2 Natted. LAN3 and LAN4 bridged'

config netmode 'routed'
        option desc 'Routed'
        option conf 'config_routed'
        option exp 'Routed changes all ports to NAT'

config netmode 'bridged'
        option desc 'Bridged'
        option conf 'config_bridged'
        option exp 'Bridged mode changes all ports bridged'
